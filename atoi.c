#include <stdio.h>
#include <stdlib.h>

/**
 * main - Start point of the program
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char *message = "Hello, Betty!\n";
int k;
for (i = 0; message[k] != '\0'; k++)
{
putchar(message[k]);
}
return (0);
}
